# Puzzles.Meta

This solution provides with APIs that can operates with variant types of puzzles, in hyper level.

This solution provides:

* Source generation (version attribute marker, primary constructor parameters, etc.)
* System extension APIs (like LINQ methods applied to type `ReadOnlySpan<T>`, extension methods on `BitArray`)
* Other APIs that is helpful in use
